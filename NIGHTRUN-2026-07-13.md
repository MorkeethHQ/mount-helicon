# Night Run — 2026-07-13

## What this is
Mount Helicon is a read-only **verifier with a memory** for AI-agent memory: it reads your store (vault/transcripts/git), runs deterministic + LLM checks for staleness/contradiction/drift, and *remembers the verdict* so ruled-out rot can't return. Tonight extended the exam from 10 rot classes to **12** — adding **R11 Identity Coherence** (as a full loop) and **R12 Phantom Association**, both grounded in the north-star's identity-drift spec.

## Shipped
**Branch:** `night-run-jul13` (off `watercolor-redesign`, **not pushed** — your gate). Commits (newest first):
- `ae2299a` **feat: R12 Phantom Association gate** — the 12th rot class.
- `76d0a11` feat: rule an identity fork from the UI (set canonical / not a fork).
- `4b0011d` feat: identity never-twice — re-alarm on a NEW divergent definition (R11 parity).
- `2f5e5ac` feat: resolve_identity — an R11 fork becomes rulable.
- `685a819` feat: R11 Identity Coherence gate — detection.
- `255b077` ux: land the app on the Zen focus review (not the slow "Reading" portrait).

**R11 — Identity Coherence (a complete loop).** Catches an entity whose *definition* forks across sources (same name, incompatible genera — "Yieldbound is a yield **treasury**" vs "a wallet **tracker**"), which R1's scalar-only gate can't see.
- Detect: article-gated defining clauses → head-noun genus; ≥2 incompatible genera across ≥2 scopes = a fork. Deterministic genus core + local-embedding semantic confirmation (drops same-concept rephrasings).
- Rule: `helicon resolve <id> --truth "<canonical def>"` (+ a UI control in the dashboard *and* the Zen focus flow) writes an approved canonical correction cube.
- Never-twice: a settled name re-alarms ONLY when a genuinely NEW divergent genus is asserted *after* the ruling — parity with the pairing gate.

**R12 — Phantom Association.** Catches a relation asserted between two entities that no source grounds ("Yieldbound rides the agent-payments wave → World"). Deterministic + high-precision: conceptual relation triples between capitalized entities (narrow verb list; code keywords excluded), flagged only when a SINGLE speculative source (idea/draft/session) asserts it with NO independent corroboration.

Both wired into `helicon audit` (rot R11/R12) and filed in `evolve`. `ROT.md`, README, and the class-count tests updated 10→12.

## Verified
**Full suite green (170 passed).** R11: 12 tests; R12: 7 tests.
```
tests/test_identity.py ............  12 passed
tests/test_relations.py .......      7 passed
170 passed, 138 warnings in 2.93s
```
**Both classes live in the audit on your real 5,705-memory store (read-only):**
```
The rot exam — 12 documented failure classes, checked live (see ROT.md)
  R11  Identity coherence           ROT FOUND
  R12  Phantom association          CLEAN
6/12 classes show rot right now · 12/12 fully tested, 0 partial
```
**Precision, the hard part** (both classes could have been noise; the gates fixed it):
- R11: raw copular matches **62 → 1** genus candidate → **0** false positives (article gate + semantic confirm). Treasury/tracker cosine 0.31 (fork) vs layer/verification-layer 0.54 (dropped).
- R12: the conceptual-verb list killed **code false positives** ("SpeechRecognitionEvent **extends** Event"); on your store, 0 phantoms — honest, no ungrounded conceptual relations currently.

## Reverted
- **R11 genus-only (no article gate):** 62 real-store findings, almost all false → killed by the article gate.
- **R12 both-endpoints-must-be-known-entities:** too tight (the 41-row entity table misses even the spec's own Yieldbound/World case) → replaced with capitalized-pair + per-pair corroboration check.
- **R12 verb "extends" et al.:** fired on TypeScript class declarations → narrowed to conceptual/business verbs only.
- **`evolve` NOT run on the live store** (writes findings/gold — your gate). All scans verified read-only.

## Needs Oscar
- **Push / merge** — everything is local on `night-run-jul13`.
- **UI action for R12 findings** — like R11 before tonight, `provenance` findings have an audit id but no `cube_id`, so they'd no-op in the review UI (dismiss works; a "real, undocumented" + "retire the phantom edge" control is unbuilt). Not active now (0 R12 findings on your store).
- No keys / money / deploy touched.

## Scorecard
**9/10.** Two genuinely new blindnesses closed (definition-fork, phantom-relation), R11 as a full detect→rule→never-twice loop from CLI and UI, all deterministic-first + tested + documented, precision hard-won. Honest gaps: (1) R12 is detection-only — no resolve/edge-retire loop yet (R11 has it, R12 doesn't); (2) R12's real-world reach depends on conceptual-relation phrasing being present (rare in a personal store) and on entity coverage; (3) semantic tiers need the optional local embedding model (graceful fallback). Nothing faked — every deterministic core stands alone.

## Next prompt
```
Close R12's loop + widen its reach, then consider the relation graph.

1. R12 resolve/dismiss: give provenance findings a real action. `--dismiss "real,
   undocumented"` already works (confirmAudit). Add resolve_relation(conn, audit_id,
   verdict): 'phantom' -> retire the asserting cube's edge + flag cubes built on it;
   'grounded' -> record it as confirmed. UI: a "phantom / real" control in FocusReview
   + FindingsView (audit id, no cube_id — route through a new /api/resolve-relation).

2. Store the asserts edge (the spec's real R12): write each relation triple as an
   edges row relation='asserts' with provenance in edges.metadata {asserted_by_cube,
   predicate, source_scope, grounding}. Then R12 reads edges, not re-extracts — and a
   dismissed phantom retires its edge. This is the architecture upgrade the spec named.

3. Broaden reach carefully: allow "X is powered by Y" (copula + rel-verb), and grow
   the entity table so more real relations qualify — WITHOUT reintroducing code noise
   (keep the conceptual-verb gate). Re-check precision on the real store each change.

Files: helicon/relations.py, helicon/graph.py (edges), helicon/api/, web review
components. Keep the suite green; verify precision read-only before filing.
```
