services:
  - type: web
    name: helicon
    runtime: docker
    plan: free
    envVars:
      - key: QWEN_API_KEY
        sync: false
      - key: HELICON_PASSWORD
        sync: false
    disk:
      name: helicon-data
      mountPath: /app/data
      sizeGB: 1
