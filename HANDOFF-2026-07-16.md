# Handoff — 2026-07-16, afternoon close

Fresh slate for tonight. Everything below is verified, not assumed.

## State: clean

- **349 tests, CI green, 0 unpushed.** HEAD `0930b25` on main.
- `helicon doctor`: all pass (nightly armed, MCP handshakes, rerank probe, static matches dist).
- Nightly fires 02:47 unattended and survives sleep (launchd, `runs=4 last exit 0`).
- Phone review live: `http://172.20.10.3:8420` (launchd KeepAlive).

## Dither — DONE, merged to main (427dfe2)

`DitherArea.tsx`: Bayer-8 canvas port of `scripts/dither_proto.py`, no dependency,
colours from CSS tokens at runtime. On GoldView (law growing, 66->104, improve
orange earned) and MemoryHealthTrend (falling, so slate not orange). Judge tab
untouched. aria-label + hidden text carry the real values. Also killed a stray
off-brand #c9a227 sparkline. 349 tests green, static synced, doctor OK.

## macOS ruling composer — IN FLIGHT (worktree)

Isolated worktree; commits safe on its branch regardless of session state.

```
git log worktree-agent-a044fa5f9fa40e8d9   # dismiss -> POST notes -> {"precedent": true}
```

If it landed commits: `git merge --no-ff <branch>`, `cd mac && swift build`,
`pytest -q`, push. If empty, it did not finish; the worktree is disposable. This
is the beat that makes the app a demo not a screenshot: rule from the native app,
watch GOLDEN_RULES recompile. Sandboxed DB; real store untouched.

## What is YOURS and blocks the submission (Jul 20 2pm PDT)

1. **FC deploy** (~15 min). Guarded and safe. Alibaba deploy is a hard
   requirement and there is still no live URL. `docker login`, export the 2 keys,
   `./fc/deploy-fc.sh`. The URL is the proof.
2. **Record the 3-min demo.** Spine below. Numbers corrected.

## The demo spine

Open on `helicon report` -> **DEGRADED**. "That is my system failing my own
threshold, and here is why that is the demo." Then the retraction: the `git log`
that killed my own feature's premise 6 hours after I shipped it. Then the loop:
rule a memory (on the Mac app if the composer landed), watch GOLDEN_RULES
recompile. Then the Qwen-vs-field result on the Judge tab.

SAY: "Qwen-plus ties Claude Sonnet 5 at 0.962, beats GPT-5 at 0.808, in a third
the latency." "Every model missed unit-drift" (your best line, real).
DO NOT SAY: "33% of agent claims are false" (n=6). "1/50th the cost" (say 8x, the
computable one). Anything about a RELAY rename outage (retracted, false).

## Ranked next (after the two blockers)

- **P1 `helicon-serve` is unwatched** — the KeepAlive daemon has no interval, so
  the routine check I built today cannot see it. A crashloop would be silent, the
  exact gap I spent the day closing. A daemon's liveness is "is the port
  answering", not "is the log fresh". ~30 min.
- **P1 D3 (from the adversarial review)** — `fossil` is computed, returned, and
  read by no verdict. A dead retrieval baseline passes forever while its own test
  docstring says "silently passing forever is how a dead test looks exactly like a
  healthy one". Real hole in the exam. Small.
- **P2 The hedge-tracker** — the wedge, measured (was a claim hedged at origin,
  did the hedge survive to the summary, to the decision). The post-deadline
  company-defining number. Better proof object than `helicon prove` (n=9,
  selection effect). Corpus exists; a documented case exists with the git log
  that disproves it.
- **P3 Kill the 4 aliases** (audit/rot, policy/gold, check/battery, repair/heal) —
  one vocabulary. And chase the `total: 1` bug in gold-history.

## Loose ends, named not hidden

- `.claude/worktrees/wf_4359e448-684-4` — 22MB, locked by a dead session.
  Gitignored so it cannot ship; unlock and `git worktree remove` it.
- The two agent worktrees above self-clean if unchanged; if they committed,
  merge then remove.

Full detail: `NIGHTRUN-2026-07-15.md` (four findings + adversarial verdict + 8-item
failure inventory), `MOONSHOT-2026-07-16.md` (experiment design).
