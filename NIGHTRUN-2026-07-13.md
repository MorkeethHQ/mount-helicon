# Night Run — 2026-07-13

## What this is
Mount Helicon is a read-only **verifier with a memory** for AI-agent memory: it reads your store (vault/transcripts/git), runs deterministic + LLM checks for staleness/contradiction/drift, and *remembers the verdict* so ruled-out rot can't return. Current state: engine solid (never-twice atom + obey path on main), a Zen review UI + Causal Lens on `watercolor-redesign`, and — as of tonight — **R11, a new rot class, built as a full loop** (detect → rule → settled).

## Shipped
**Branch:** `night-run-jul13` (off `watercolor-redesign`, **not pushed** — your gate). Commits:
- `255b077` ux: land the app on the Zen focus review, lead the rail with it (fixes your "it's the same?" — you were landing on the slow LLM "Reading" portrait, never reaching the focus review).
- `685a819` **feat: R11 Identity Coherence gate** — detection.
- `2f5e5ac` **feat: resolve_identity** — makes an R11 fork *rulable*, closing the loop.

**R11 — Identity Coherence** (north-star's designated next lens). Catches an entity whose *definition* forks across sources (same name, incompatible genera — "Yieldbound is a yield **treasury**" vs "a wallet **tracker**"), which R1's scalar-only contradiction gate is structurally blind to.
- `helicon/identity.py`: `extract_glosses` / `find_identity_forks` / `identity_scan` / `resolve_identity`.
- **Precision core = an article gate**: a real definition is "X is **a** <genus>"; status prose ("Relay is live") has no article and is dropped. Two tiers — deterministic genus core (the rot-exam path) + optional local-embedding semantic confirmation for filing (drops same-concept rephrasings, cosine <0.45).
- **The full loop:** `evolve` files a confirmed fork → `helicon resolve <id> --truth "<canonical def>"` writes an approved canonical-identity correction cube (provenance) + closes it → `find_identity_forks` then skips the settled name. Wired as rot **R11**; documented in `ROT.md`; README + tests updated (10→11 classes, endpoints 81→82).

## Verified
**Full suite green (161 passed)** and **10 R11 tests pass** (article gate; head-noun genus; cross-source fork fires; same-genus / single-source / status-prose do NOT fire; idempotent filing; rot-exam reports R11; resolve settles the fork + a re-scan files nothing; bad-input rejected):
```
tests/test_identity.py ..........  10 passed in 0.18s
161 passed, 138 warnings in 2.46s
```
**R11 live in the audit on your real 5,424-memory store (read-only):**
```
The rot exam — 11 documented failure classes, checked live (see ROT.md)
  R11  Identity coherence           ROT FOUND
6/11 classes show rot right now · 11/11 fully tested, 0 partial
```
**Semantic tier separates true forks from rephrasings** (embedding cosine): treasury/tracker **0.311** (< 0.45 → flag) vs verification-layer/layer **0.538** and treasury/yield-vault **0.567** (→ dropped). On your real store: genus tier → 1 candidate (from a *truncated cube in the store*), semantic → **0 confirmed**. No noise added to your review queue.

## Reverted
- **Genus-only R11 (no article gate):** 62 findings on the real store, almost all false ("relay: live vs shipped", "commit: feat vs fix") — would have recreated the exact overwhelm you flagged. Killed by the article gate + adjective stoplist + whole-word predicate extraction (62 → 1 → 0).
- **`evolve` NOT run on the live store** (it writes findings/gold — your gate). `identity_scan` verified read-only instead.

## Needs Oscar
- **Push / merge** — everything is local on `night-run-jul13`; push or fold into `watercolor-redesign` / `main` at your call.
- **Identity findings have no UI action yet** — a filed fork appears in the review queue, but FocusReview/FindingsView route Confirm/Retire through the cube-review path (identity findings have an audit id, no `cube_id`), so they'd no-op in the UI. Not active now (0 confirmed on your store). Backend rule path (`helicon resolve <id> --truth …`) works today.
- No keys / money / deploy touched.

## Scorecard
**9/10.** R11 is a real, tested, precise, documented new rot class that closes a blindness R1 structurally has — and it's a *full loop* (detect → rule → remembered), not just a detector. Honest gaps: (1) the never-twice guard is basic — a resolved fork stays settled, but a genuinely NEW divergent genus asserted after the ruling does not yet re-alarm (the pairing gate does this; identity doesn't yet); (2) no UI action for identity findings; (3) the semantic tier needs the optional local embedding model (graceful genus-only fallback if absent). Nothing is faked — the deterministic core stands alone.

## Next prompt
```
Finish R11 to parity with the pairing gate + surface it in the UI.

1. Identity never-twice (re-alarm on divergence): in find_identity_forks, instead of
   skipping a resolved name outright, load the resolution (canonical genus + resolved_at)
   and re-alarm ONLY if a cube created_at > resolved_at asserts a genus != canonical.
   Track created_at per gloss (add it to the by_name records). Mirror
   pairing.find_conflicts:278-316. Test: resolve "Yieldbound = treasury"; a later
   "is a wallet tracker" cube re-files; a later "is a treasury" cube does not.

2. UI: give identity findings a real action in FocusReview + FindingsView. They carry
   an audit id but no cube_id, so route Confirm→dismiss (confirmAudit 'dismissed') and
   add a "set canonical" inline input that POSTs to a small /api/resolve-identity
   endpoint calling resolve_identity. Verify a fork can be ruled from the Zen focus flow.

3. Then R12 (phantom association) — the larger follow-on: relation triples with
   provenance in edges.metadata; flag single-source speculative relations. See
   helicon-identity-drift-gate-2026-07-12.md.

Files: helicon/identity.py, helicon/pairing.py (never-twice pattern), helicon/api/,
web/src/components/FocusReview.tsx + FindingsView.tsx. Keep the suite green.
```
