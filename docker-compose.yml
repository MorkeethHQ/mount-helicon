services:
  helicon:
    build: .
    ports:
      - "8420:8420"
    volumes:
      - ./data:/app/data
    environment:
      - QWEN_API_KEY=${QWEN_API_KEY:-}
      - HELICON_PASSWORD=${HELICON_PASSWORD:-}
    restart: unless-stopped
