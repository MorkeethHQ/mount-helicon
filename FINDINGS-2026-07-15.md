# Findings for the Helicon terminal — from the orchestrator, 2026-07-15

Four findings, each verified live today, each with the command that proves it. Ranked by
what a Jul-20 judge would find in thirty seconds.

Context worth having: **today Helicon caught its own blindness six times.** Its nightly
silently skipped, its law compiled a blank rule, its ruling loop poisoned itself, and a
dead name it had been reporting as "hygiene" turned out to have blacked out FAVOUR's
entire agent layer for 13 days. That is not embarrassing, it is the pitch. A verifier
that indicts itself on camera is the demo — nobody can accuse it of a staged result.

---

## 1. 🔴 R8 — retrieval regressed on 11 of 13 snapshots. Face this first.

```
$ python3 -m helicon.cli rot
  R8  Retrieval regression        ROT FOUND
      11/13 snapshot(s) regressed vs baseline

$ python3 -m helicon.cli report
  Overall: DEGRADED  (battery: 6 healthy / 6 degraded / 1 broken of 13 tasks)
  1. Efficient storage & retrieval   HEALTHY   P@3 0.615  MRR 0.596
```

**Why it ranks first:** this is a *MemoryAgent* hackathon. Retrieval quality is the
product's core claim, `report` prints DEGRADED, and a judge can run that command. The
Runs dashboard tab is polish on top of a number that is moving the wrong way. Five days
left. If it can't be fixed, the honest move is to *name* it in the submission before a
judge finds it — a system that reports its own degradation is on-thesis; one that hides
it is just another benchmark.

## 2. 🔴 The ruling loop poisons itself (a Jul-6 regression)

`resolve --list` shows **#361 Itai wedding: 08-14..08-22 vs 09-11..09-13** as a live
critical contradiction. It was already ruled on Jul 11 (#322) and the ruling is compiled
into GOLDEN_RULES. The source of the "wrong" side:

```
$ grep -n "08-14" memory/status_2026-07-11.md
16:   - #322 Itai wedding = **09-11..09-13** (the "Aug 14-22" cube was the Italy trip mislabeled)
```

That line **is the correction**. The parser lifts the dead value out of the sentence
written to kill it and files a fresh contradiction. GOLDEN_RULES already carries a
precedent for this exact class ("false positive: LOUPE correction banner parsed as the
'open' side (both-poles line, fixed Jul 6)") — so the Jul-6 fix does not cover
`X was mislabeled as Y` phrasing.

**Consequence: every ruling Oscar makes plants a future false alarm.** That inflates R1
forever and quietly discredits the whole rot exam, which is the one thing that must stay
trustworthy. Fix the both-poles parser for correction-phrasing, add #361 as a regression
test, then re-scan and see how much of R1's "9 live conflicts" evaporates.

## 3. 🟠 ~~R4 rename rot blacked out a production system for 13 days~~ **RETRACTED — see the correction below**

> **CORRECTION 2026-07-15, later the same day. The finding below is WRONG about its cause, and
> the error is mine (the orchestrator's), not the FAVOUR lane's.**
>
> **What is true:** the agent-layer blackout is real and still live. 41 of 41 agent-posted
> production tasks carry `agent: null`. Eight named agents have never fired.
>
> **What is false: the RELAY→FAVOUR rename did not cause it.** Verified across all 15 commits
> that ever touched `src/lib/agents.ts`: **`"relay"` was never a key in `AGENT_REGISTRY`, in any
> commit, ever.** The only historical hits are `sender: "relay-bot"` message strings and a prose
> comment. Further: **12 of the 41 tasks broke *before* the rename**, while "relay" was still the
> live, correct product name. And `getAgent("favour")` returns null too, because `favour` is not
> a key either. **The "fix" this finding implies — update the dead name — reproduces the outage
> under a live name.**
>
> **The real cause** is the one the FAVOUR lane actually identified and which I overwrote:
> `agent:` carries two unrelated meanings in one namespace. `agent:relay` means *"the platform
> seeded this"* (its only job is the admin exemption); `agent:shelfwatch` means *"a registry agent
> posted this"*. Privilege keys off `poster.startsWith("agent:")` and is never validated against
> the registry, so concept 1 silently satisfies concept 2's code path. That is a **namespace
> collision, not rename rot**, and the FAVOUR lane's fix (split seeding privilege from agent
> identity, `ea2548c`) is correct and already shipped.
>
> **How this got here, because it is the more useful finding:** the FAVOUR lane wrote a hedge —
> *"`agent:relay` is also a dead name, which is **likely** how it rotted unnoticed"* — and I
> promoted "likely" to a headline, then propagated it to the dashboard, the registry, and this
> file. It then motivated a real feature (`11f0ad1`). **A hedge laundered through a summary
> becomes a fact, and nothing in the pipeline preserves the uncertainty.** That is the same
> failure class as everything else on this list: not a false report, a *confident* one.
>
> **What survives:** the R4 *capability* (naming code leads with file:line instead of printing a
> count) is still worth having — a dead name in a code path genuinely is a different class from
> one in prose. But **its motivating exemplar is misattributed**, and R4 must not claim this
> outage as a rename. Reclassify it, and if no other code-path exemplar exists, say so.
>
> Full evidence: `DEAD-NAME-SWEEP-2026-07-15.md` (independently re-derived, verdict CONFIRMED).

## 3b. (the original text, retained so the error is legible rather than deleted)

```
$ python3 -m helicon.cli rot
  R4  Supersession / rename       ROT FOUND
      RELAY->FAVOUR: 606 live dead-name ref(s) = 92 history + 190 rename-aware + 324 current-claim(s)
```

Today the FAVOUR terminal found the consequence (`world-relay/BUILD-PLAN-2026-07-15.md`):
all **107** production tasks carry `agent: null`. Cause chain: tasks post as
`poster: "agent:relay"` → strips to `agentId="relay"` → `getAgent("relay")` → no such key
in `AGENT_REGISTRY` → returns null, **silently, no error, no log**. `agent:relay` is a
dead name (RELAY→FAVOUR, renamed 2026-07-02). Eight named agents — ShelfWatch, FreshMap,
QueuePulse, PropertyCheck, DropScout, OpenClaw, Hermes, Claude Code — have never once
fired, and their differentiated `verificationPrompt` has never run.

**The finding for Helicon:** it has been reporting 324 current-claim dead names as a
count. At least one of them was load-bearing in running code. **Dead names in prose are
rot; dead names in a code path are an outage.** That distinction doesn't exist in R4
today, and adding it turns a lint into an incident detector. Concretely: cross-reference
dead names against live code paths, env vars, and configs, and rank those separately.
This is also the single best demo beat you have — "the rename checker found a
thirteen-day production blackout."

## 4. 🟡 `gold.py:131` — a memory with empty frontmatter compiles into the law as a blank rule

`feedback_ambition_past_done.md` had `name: ""` and no description. Its stored cube title
was empty. `gold.py:131` does `g["feedback"].append({"rule": title[:120], ...})`, so the
law rendered:

```
-   
  _[feedback_ambition_past_done.md]_
```

**The rule telling agents not to stop early was loading into every session as a blank
line, and nothing complained.** I repaired the data (frontmatter + the cube title) and
recompiled — the rule now reads correctly. **The code is still wrong.** Fix the compile
path (fall back to summary/content, or refuse to emit an empty rule and warn loudly),
add a regression test, then sweep: are any other rules compiling blank or truncated at
120 chars mid-word? A law with a silent hole is worse than no law, because it is trusted.

Related: re-ingest does not refresh a cube's title when only frontmatter changes (the
content hash doesn't move), so I had to UPDATE the cube directly. That's arguably a
second bug — a memory can be edited and the store won't notice.

---

## Also: the thing Oscar actually asked for exists and is thin

He said today: *"mainly i need to review output from sitting in 5 terminals at the same
time.. scouting through brave is not optimal."* That is `helicon review --terminals`,
which you built Jul 14. Run against six live terminals it returned **2 claims**, both
`[unverified]` with "test files present, claimed count NOT re-run - add --run".

It reads committed closeouts, not live terminal state, and nobody passes `--run`, so
nothing is actually verified. `helicon-context-feed` already publishes it every 30
minutes — the pipe exists; the coverage and the verification don't.

**This is simultaneously the product's sharpened north star (registry #017, Oscar's own
Jul-13 call) and the thing he is doing by hand anyway. Today is the best test data it
will ever get.** After R8, this is where I'd point.

---

*Every claim above has a command. Verified by the orchestrator, 2026-07-15. Nothing here
is inferred from a doc.*
